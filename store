<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ultimate DaVinci Resolve Template | Muk Post</title>
    
    <link rel="icon" href="https://i.imgur.com/0czpN64.png" type="image/png">
    <meta property="og:title" content="Ultimate DaVinci Resolve Template | Muk Post">
    <meta property="og:description" content="The professional pro-grade template for editing, colour, sound, and delivery.">
    <meta property="og:image" content="https://i.imgur.com/p5t4Fk0.png">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <!-- Gumroad Overlay Script -->
    <script src="https://gumroad.com/js/gumroad.js"></script>

    <style>
        /* CORE DESIGN SYSTEM */
        :root {
            --bg: #0a0a0a;
            --surface: #111111;
            --surface-hover: #161616;
            --text-main: #eeeeee;
            --text-dim: #888888;
            --accent: #ffffff;
            --border: #1e1e1e;
            --font: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            font-family: var(--font);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            overflow-x: hidden;
        }

        .container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; }
        
        /* NAV (Matching Index V1.2) */
        nav {
            position: fixed; top: 0; width: 100%; height: 80px;
            background: rgba(10, 10, 10, 0.85); backdrop-filter: blur(20px);
            z-index: 1000; display: flex; align-items: center;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        nav .container { display: flex; justify-content: space-between; align-items: center; width: 100%; }
        .nav-logo { width: 44px; height: auto; }
        .nav-right { display: flex; align-items: center; }
        .nav-links a { color: var(--text-dim); text-decoration: none; text-transform: uppercase; font-size: 0.75rem; font-weight: 600; letter-spacing: 0.15em; margin-left: 2.5rem; transition: 0.3s; }
        .nav-links a:hover { color: var(--accent); }

        /* PRODUCT HERO SECTION */
        .product-hero {
            padding-top: 160px;
            min-height: 90vh;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .product-info {
            animation: fadeUp 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        .category {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            color: var(--text-dim);
            margin-bottom: 1.5rem;
            display: block;
        }

        .product-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            text-transform: uppercase;
            line-height: 1.1;
            letter-spacing: -0.02em;
            margin-bottom: 1.5rem;
        }

        .product-price {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--accent);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .price-badge {
            font-size: 0.7rem;
            padding: 0.4rem 0.8rem;
            border: 1px solid var(--border);
            border-radius: 4px;
            letter-spacing: 0.15em;
            text-transform: uppercase;
            color: var(--text-dim);
        }

        .product-desc {
            color: var(--text-dim);
            font-size: 0.95rem;
            max-width: 480px;
            margin-bottom: 3rem;
            line-height: 1.8;
        }

        /* GUMROAD BUTTON */
        .buy-btn {
            display: inline-block;
            background: var(--accent);
            color: #000;
            text-decoration: none;
            padding: 1.2rem 3.5rem;
            font-weight: 800;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
            box-shadow: 0 0 20px rgba(255,255,255,0.1);
        }
        .buy-btn:hover {
            background: #d0d0d0;
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(255,255,255,0.2);
        }

        /* 3D BOX ANIMATION */
        .product-visual {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            perspective: 1200px;
        }

        .box-container {
            width: 280px;
            height: 380px;
            position: relative;
            transform-style: preserve-3d;
            animation: spinBox 15s infinite linear;
        }

        .box-face {
            position: absolute;
            width: 100%;
            height: 100%;
            background: rgba(17, 17, 17, 0.9);
            border: 1px solid rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            box-shadow: inset 0 0 40px rgba(0,0,0,0.8);
        }

        .box-front { transform: translateZ(35px); }
        .box-back { transform: rotateY(180deg) translateZ(35px); }
        .box-left { width: 70px; transform: rotateY(-90deg) translateZ(35px); background: #0a0a0a; }
        .box-right { width: 70px; transform: rotateY(90deg) translateZ(245px); background: #0a0a0a; }
        .box-top { height: 70px; transform: rotateX(90deg) translateZ(35px); background: #111; }
        .box-bottom { height: 70px; transform: rotateX(-90deg) translateZ(345px); background: #111; }

        .box-logo { width: 60px; margin-bottom: 1.5rem; opacity: 0.8; }
        .box-title { font-weight: 800; font-size: 1.8rem; text-align: center; text-transform: uppercase; line-height: 1; letter-spacing: -0.02em; }
        .box-subtitle { font-size: 0.6rem; text-transform: uppercase; letter-spacing: 0.3em; color: var(--text-dim); margin-top: 1rem; }
        
        /* Studio Glow under the box */
        .box-glow {
            position: absolute;
            bottom: -50px;
            left: 50%;
            transform: translateX(-50%) rotateX(90deg);
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(0,0,0,0) 70%);
            pointer-events: none;
        }

        @keyframes spinBox {
            0% { transform: rotateY(0deg) rotateX(5deg); }
            50% { transform: rotateY(180deg) rotateX(-5deg); }
            100% { transform: rotateY(360deg) rotateX(5deg); }
        }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* FEATURES GRID */
        .features-section { padding: 120px 0; border-top: 1px solid var(--border); }
        .features-grid {
            display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; margin-top: 4rem;
        }
        .feature-card {
            background: var(--surface); padding: 2.5rem; border-top: 1px solid var(--border); transition: 0.3s;
        }
        .feature-card:hover { border-top-color: var(--accent); transform: translateY(-5px); }
        .feature-card h3 { font-size: 1.1rem; text-transform: uppercase; margin-bottom: 1rem; letter-spacing: 0.05em; }
        .feature-card p { font-size: 0.85rem; color: var(--text-dim); line-height: 1.6; }

        /* FOOTER (Matching Index V1.2) */
        footer { padding: 120px 0 80px; text-align: center; border-top: 1px solid var(--border); margin-top: 80px; }
        .footer-logo { width: 55px; margin: 0 auto 2rem; opacity: 0.8; }

        @media (max-width: 900px) {
            .product-hero { grid-template-columns: 1fr; text-align: center; padding-top: 120px; }
            .product-desc { margin: 0 auto 3rem; }
            .product-price { justify-content: center; }
            .product-visual { height: 400px; margin-top: 2rem; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="container">
            <a href="index.html">
                <img src="https://i.imgur.com/0czpN64.png" alt="M Logo" class="nav-logo">
            </a>
            <div class="nav-right">
                <div class="nav-links">
                    <a href="index.html#about">Workflow</a>
                    <a href="index.html#work">Work</a>
                    <a href="index.html#contact">Contact</a>
                </div>
            </div>
        </div>
    </nav>

    <main>
        <section class="product-hero container">
            <div class="product-info">
                <span class="category">Muk Post Digital Assets</span>
                <h1 class="product-title">Ultimate DaVinci Resolve Studio Template</h1>
                
                <div class="product-price">
                    $49.99 <span class="price-badge">Studio License</span>
                </div>
                
                <p class="product-desc">
                    A professional-grade DaVinci Resolve project framework built for speed and precision. Pre-configured for advanced colour management, complex node structures, and broadcast-ready Fairlight audio routing. Skip the setup and start finishing.
                </p>

                <!-- GUMROAD BUTTON PLACEHOLDER -->
                <!-- Replace 'your-product-link' with your actual Gumroad URL when ready -->
                <a class="gumroad-button buy-btn" href="https://gumroad.com/l/your-product-link" data-gumroad-overlay-checkout="true">
                    Purchase Template
                </a>
            </div>

            <div class="product-visual">
                <div class="box-container">
                    <div class="box-glow"></div>
                    <div class="box-face box-front">
                        <img src="https://i.imgur.com/0czpN64.png" alt="Logo" class="box-logo">
                        <div class="box-title">Ultimate<br>Resolve<br>Template</div>
                        <div class="box-subtitle">Edit • Colour • Sound</div>
                    </div>
                    <div class="box-face box-back"></div>
                    <div class="box-face box-left"></div>
                    <div class="box-face box-right"></div>
                    <div class="box-face box-top"></div>
                    <div class="box-face box-bottom"></div>
                </div>
            </div>
        </section>

        <section class="features-section container">
            <h2 style="font-size: 2rem; text-transform: uppercase; font-weight: 800; letter-spacing: -0.02em; text-align: center;">What's Included in the Timeline</h2>
            
            <div class="features-grid">
                <div class="feature-card">
                    <h3>01. Custom Node Trees</h3>
                    <p>Pre-built color space transforms, OFX film look creator routing, halation, and grain layers optimized for cinematic look development.</p>
                </div>
                <div class="feature-card">
                    <h3>02. Fairlight Audio Buses</h3>
                    <p>Pre-routed submixes for dialogue cleanup, sound design, and master bus limiting. Ready for your EQ and reverb plugins.</p>
                </div>
                <div class="feature-card">
                    <h3>03. Smart Bins & Organization</h3>
                    <p>Strict folder hierarchies and smart bins to automatically sort your raw media, audio stems, and graphics elements upon import.</p>
                </div>
                <div class="feature-card">
                    <h3>04. Export Presets</h3>
                    <p>Standardized delivery configurations including Frame.io client review specs, YouTube 4K, and high-fidelity archival masters.</p>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <img src="https://i.imgur.com/0czpN64.png" alt="M Logo" class="footer-logo">
        <p style="color: var(--text-dim); font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.15em;">&copy; 2026 Muk Post. All Rights Reserved.</p>
    </footer>

</body>
</html>
